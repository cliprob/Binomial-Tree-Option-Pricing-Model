# Binomial-Tree-Option-Pricing-Model
Model that valuates European/American Call/Put Options with CRR (Cox, Ross and Rubenstein) Method and uses numpy arrays to compute the American Option Valuations faster then when using for loops.
